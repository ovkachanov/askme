<div class="question">
  <div class="question-content">
    <div class="question-text text-lead">
      <%= render_hashtag_link_question(question.text) %>
    </div>

    <% if question.answer.present? %>
      <div class="question-answer text-lead">
        <%= render_hashtag_link_answer(question.answer) %>
      </div>
    <% end %>

    <% if question.author.present? %>
      <span class="text-small">Автор: </span>
      <%= link_to "@#{question.author.username}", user_path(question.author) %>
    <% else %>
      anonymous
    <% end %>

    <div class="question-date">
      <%= l(question.created_at, format: :short) %>
    </div>
  </div>

  <% if question.user == current_user %>
    <% edit_icon = question.answer.blank? ? 'reply' : 'pencil' %>

    <div class="question-actions">
      <%= link_to fa_icon(edit_icon), edit_question_path(question) %>
      <%= link_to fa_icon('trash-o'), question_path(question), method: :delete, data: {confirm: 'Точно удалить вопрос?'} %>
    </div>
  <% end %>
</div>
